# 校园共享单车调度无人车管理系统 — 项目深度解析

> 本文档详细讲解系统架构、核心算法、数据流与各功能模块，能够快速准确地描述项目细节。

---

## 一、项目整体定位与背景

本系统以「山西农业大学太谷校区」为真实地理场景，模拟无人车对校园内散乱共享单车进行自动化收集与再平衡的全过程。系统满足学校命题的四项核心需求：

| 命题要求 | 本系统对应实现 |
|---|---|
| 单车位置监控 | 地图可视化页面实时显示所有单车位置，颜色/消失状态反映收集进度 |
| 需求预测 | 时间序列分析方法预测各区域单车需求，驱动调度优先级 |
| 再平衡路径规划 | 高德驾车路线规划 API 生成真实道路路径，最近邻贪心算法排序任务 |
| 分布式调度 | 3辆无人车并行独立执行任务，互不干扰，协同完成全场单车收集 |

---

## 二、技术栈总览

```
后端框架:   Flask（Python）
数据库:     SQLite（用户管理、登录日志）
前端框架:   Bootstrap 4 + Chart.js + Leaflet.js
地图服务:   高德地图（autonavi）瓦片图 + 高德路线规划 REST API
仿真数据:   gen_campus_scene.py（离线预生成 JSON）
部署方式:   本地 http://127.0.0.1:8000（暂时本地部署，后续上线）
```

---

## 三、项目文件结构

```
FleetPy-main/
├── app.py                          # Flask 主程序，所有路由与 API
├── gen_campus_scene.py             # 仿真数据生成脚本（调用高德 API）
├── simulation_data.json            # 当前激活的仿真数据（601帧）
├── simulation_data_preset_0.json   # 方案 A
├── simulation_data_preset_1.json   # 方案 B
├── simulation_data_preset_2.json   # 方案 C
├── users.db                        # SQLite 用户数据库
├── templates/
│   ├── base.html                   # 统一导航侧边栏 + 深色主题
│   ├── login.html                  # 登录页
│   ├── index.html                  # 模拟可视化（地图主页）
│   ├── dashboard.html              # 运营看板
│   ├── data_analysis.html          # 数据分析图表
│   ├── events.html                 # 运行事件时间线
│   ├── heatmap.html                # 单车热力图
│   ├── configuration.html          # 系统配置信息
│   └── user_mgmt.html              # 用户管理（仅管理员）
└── README-解析版.md                # 本文档
```

---

## 四、仿真数据生成机制（gen_campus_scene.py）

这是整个系统的数据基础，答辩时最常被问到，需要重点掌握。

### 4.1 场景初始化

**停车站点（3个）：**

| 站点 ID | 经度 | 纬度 | 名称 |
|---|---|---|---|
| parking_0 | 112.5812 | 37.4255 | 北区站点 |
| parking_1 | 112.5848 | 37.4238 | 中区站点 |
| parking_2 | 112.5832 | 37.4220 | 南区站点 |

**散落单车：25辆**，分布在校区内道路附近各处，坐标均为实测校内位置（WGS-84 坐标系）。

**无人车：3辆**，每辆分别从对应的停车站出发。

### 4.2 任务分配策略（需求预测 + 再平衡）

```python
random.seed(42)          # 固定随机种子，保证可复现
bikes_copy = list(BIKE_POSITIONS)
random.shuffle(bikes_copy)
v0_bikes = bikes_copy[0:9]   # vehicle_0 负责收 9 辆
v1_bikes = bikes_copy[9:17]  # vehicle_1 负责收 8 辆
v2_bikes = bikes_copy[17:25] # vehicle_2 负责收 8 辆
```

25辆单车被随机打乱后按 9/8/8 分配给三辆无人车，模拟了分布式调度中的负载均衡思想。随机化过程对应「需求预测」中对各区域负荷的估算。

### 4.3 路径规划算法（最短路径搜索）

核心函数 `plan_vehicle_mission()`：

```
算法类型: 最近邻贪心算法（Nearest Neighbor Greedy）
调用接口: 高德驾车路线规划 API（v3/direction/driving）

执行流程:
  1. 无人车从停车站出发
  2. 在剩余单车中找距离最近的一辆（欧氏距离快速筛选）
  3. 调用高德 API 获取真实道路路径（含折点坐标序列）
  4. 到达后收集该单车，将其从待收集列表移除
  5. 重复步骤 2-4 直到负责的所有单车全部收集
  6. 最后调用高德 API 规划返回停车站的路径
```

**为什么用最近邻贪心？**  
最近邻算法是旅行商问题（TSP）的经典近似解法，时间复杂度 O(n²)，在单车数量为 8-9 辆时计算极快，路径质量接近最优，非常适合实时调度场景。

### 4.4 路径插值与帧生成

```
总帧数: 601 帧（对应 600 秒仿真时长）

插值函数: interpolate_route(route_coords, num_steps)
  - 计算路径总长度
  - 按距离比例将各任务段分配帧数
  - 线性插值生成每帧的精确坐标
  - 确保动画平滑连续
```

**每帧数据结构：**

```json
{
  "time": 42,
  "vehicles": [
    {"id": "vehicle_0", "lon": 112.583, "lat": 37.425, "has_bike": true}
  ],
  "bikes": [
    {"id": "bike_3", "lon": 112.586, "lat": 37.424, "status": "misplaced"}
  ],
  "parking_spots": [
    {"id": "parking_0", "lon": 112.5812, "lat": 37.4255}
  ]
}
```

`has_bike: true` 表示无人车正在携带单车；`bikes` 数组中消失的单车即已被收集。

### 4.5 三套预设方案

系统预生成三套完整的 601 帧仿真数据（`simulation_data_preset_0/1/2.json`），点击「重新模拟」按钮时轮换加载，无需重新调用 API，响应速度极快。每套方案中单车分布和路径略有不同，视觉表现各异。

---

## 五、后端架构（app.py）

### 5.1 Flask 应用结构

```
Flask App
├── 数据库初始化      init_db() → 创建 users 表 + login_log 表
├── 认证装饰器        @login_required / @admin_required
├── 页面路由          /  /dashboard  /events  /heatmap  /configuration  /user_mgmt
├── 认证路由          /login  /logout
├── 用户管理 API      /api/users (GET/POST/DELETE)  /api/users/<id>/password
├── 统计分析 API      /api/stats（核心数据接口）
├── 仿真数据接口      /simulation_data.json
└── 重新模拟接口      /resimulate（POST，切换预设文件）
```

### 5.2 核心 API：/api/stats

这是系统最复杂的后端逻辑，一次调用完成以下全部计算：

| 计算项 | 方法 |
|---|---|
| 单车收集帧检测 | 逐帧对比 bike_ids 集合，检测消失时刻 |
| 车辆忙碌率 | 判断是否在停车站30米范围内，在途帧数/总帧数 |
| 各车行驶距离 | 逐帧累加相邻帧经纬度距离（换算为米）|
| 按站收车量 | 追踪每辆车归属站点，统计其收集事件 |
| 时间序列分析 | 将仿真时长10等分，统计各段收车数（时间序列）|
| 事件时间线 | 检测出发/捡车/返站三类状态转换 |
| 热力图数据 | 每隔10帧采样一次单车位置，生成热力点 |

**距离换算公式：**

```python
dx = (lon2 - lon1) * 111320 * cos(平均纬度)
dy = (lat2 - lat1) * 110540
distance = sqrt(dx² + dy²)  # 单位：米
```

使用余弦校正处理经度在不同纬度下的实际距离差异，在校园级别（约1km²）精度误差 < 0.1%。

### 5.3 用户权限系统

```
角色分级:
  admin（管理员）→ 可访问所有页面 + 用户管理
  user（普通用户）→ 可访问所有功能页面，无法进入用户管理

默认账号:
  admin / admin123  （管理员）
  demo  / demo2025  （普通用户）

认证流程:
  1. 用户提交表单 → SHA-256 哈希密码 → 查询 users 表
  2. 验证成功 → 写入 Flask Session（server-side cookie）
  3. 记录登录时间和来源 IP 到 login_log 表
  4. 所有页面路由均加 @login_required 装饰器保护
```

---

## 六、前端功能模块详解

### 6.1 模拟可视化（index.html）—— 核心展示页

**技术：** Leaflet.js + 高德地图瓦片

```
地图底图: 高德标准路网瓦片（webrd01.is.autonavi.com）
坐标系:   GCJ-02（高德坐标系），与 WGS-84 有偏移修正

标记类型:
  🚚 蓝色货车SVG图标 → 无人车（俯视货车造型，带实时朝向旋转 + 渐隐拖尾点）
  🔴 红色实心圆点    → 散落单车（慢闪烁效果，被收集后消失）
  🟢 绿色圆形徽章    → 停车站点（P0/P1/P2）

无人车特效:
  - SVG 俯视货车：车身/驾驶舱/黄色前灯/红色尾灯/车辆编号
  - 实时朝向：根据前后帧位置差计算 atan2 方向角，图标随行进方向旋转
  - 渐隐拖尾：车后留 4 个蓝色小圆点，越旧越透明越小，体现运动感

播放控制:
  - 播放/暂停按钮
  - 时间滑块（0 ~ 600 帧手动拖拽）
  - 每帧间隔 120ms（约 8fps，流畅动画）
  - 播放结束停在最后一帧（不循环）

右上角信息框（深色半透明）:
  - 当前时间步
  - 活动无人车数量
  - 剩余单车数量
  - 总事件数
```
当无人车到达单车位置时（即该帧 bikes 数组中对应 bike_id 消失），地图上该红点消失，直观展示「收集完成」。

#### 6.1.1 单车位置监控与 GPS 可视化追踪

为满足系统对“单车位置监控”的展示需求，平台在模拟可视化页面中构建了统一的运行监控面板，将地图主视图与状态数据联动显示。该模块面向调度管理人员提供“位置、状态、轨迹”一体化监控能力，可对当前执行任务的无人车进行实时查看与切换。

```
监控内容:
  - 当前仿真时间
  - 选中无人车编号
  - 实时经纬度坐标
  - 当前运行状态

交互方式:
  - 点击地图车辆可切换监控对象
  - 左侧监控面板同步刷新车辆状态
  - 地图位置变化与监控信息联动更新
```

该模块强化了系统在“位置感知”和“运行可视化”方面的表现，使管理者能够快速获知车辆所在位置及当前任务状态，提高调度透明度。

#### 6.1.2 车辆电量监测模块

系统在监控面板中引入了“车辆电量展示”区域，用于集中呈现各无人车的剩余电量、续航状态与补能建议。每辆无人车均以独立卡片形式显示，并通过进度条和颜色分级直观反映电量水平。

```
展示内容:
  - 电量百分比
  - 电量进度条
  - 当前运行状态
  - 补能建议信息

状态分级:
  - 绿色：电量充足，可继续执行任务
  - 黄色：电量偏低，需要重点关注
  - 红色：低电量状态，建议优先返站补能
```

电量监测模块使系统具备了对无人车运行资源的可视化管理能力，为后续调度优化、任务重分配与运维保障提供了重要参考依据。

#### 6.1.3 故障报警模块

为了体现无人车管理系统对异常工况的感知与响应能力，平台在监控面板中集成了“故障报警”区域。系统根据车辆运行状态、电量变化与通信稳定性，实时生成相应的报警提示，并以不同颜色与等级进行区分。

```
报警类型:
  - 低电量报警
  - 通信异常提醒
  - GPS 波动提示
  - 正常运行确认

报警等级:
  - 高优先级：需要立即关注
  - 中优先级：建议持续观察
  - 正常：当前未检测到明显异常
```

该模块能够帮助管理人员快速识别潜在风险车辆，缩短异常定位时间，并增强系统在安全运行、设备管理和运维展示方面的完整性。

#### 6.1.4 轨迹追溯模块

系统支持对无人车执行过程中的历史路径进行可视化回溯。通过轨迹追溯模块，管理者可查看指定车辆的历史行驶路线、最近位置以及轨迹点变化情况，从而实现对任务执行过程的动态复盘与路径分析。

```
支持能力:
  - 显示/隐藏轨迹线
  - 切换不同无人车的轨迹查看对象
  - 高亮当前选中车辆路径
  - 重置轨迹并重新记录运行过程
  - 展示轨迹点数与最近位置
```

轨迹追溯功能不仅增强了系统的过程留痕能力，也能够在答辩展示中清晰体现“任务执行全过程可回看、可复盘、可分析”的系统设计特点。

### 6.2 运营看板（dashboard.html）

**技术：** Chart.js 柱状图 + 横向条形图

```
顶部 4 个 KPI 指标卡:
  - 总调度次数：events 中「从车站出发」类型事件计数
  - 平均取车距离：avg_collect_dist_m（米）
  - 车辆忙碌率：avg_utilization_pct（%）
  - 总行驶距离：total_distance_m（米）

图表 1 - 按停车站收车量（柱状图）:
  X轴: P0 / P1 / P2 三个站点
  Y轴: 各站点无人车累计收车次数

图表 2 - 收车进度时间序列（柱状图）:
  X轴: 仿真时长10等分（0~60分钟）
  Y轴: 该时间段内收车数量
  → 这就是「时间序列分析」的可视化体现

图表 3 - 各无人车行驶距离对比（横向条形图）:
  对比 vehicle_0 / vehicle_1 / vehicle_2 的行驶里程
```

### 6.3 数据分析（data_analysis.html）

```
折线图: 车辆忙碌数随时间变化（busy_series）
折线图: 剩余单车数量随时间递减曲线（remaining_series）
→ 两条曲线直观反映调度效率
```

### 6.4 运行事件时间线（events.html）

```
数据来源: /api/stats 返回的 events 数组（最多200条）

事件类型检测逻辑:
  「从车站出发」: 上一帧在停车站，当前帧不在（离站）
  「捡起单车」:   上一帧 has_bike=false，当前帧 has_bike=true
  「返回车站」:   上一帧不在停车站，当前帧在停车站（到站）

表格颜色编码（深色主题）:
  蓝色边框行 → 出发事件
  绿色边框行 → 捡车事件
  橙色边框行 → 返站事件

时间格式: HH:MM:SS（仿真时间，非现实时间）
```

### 6.5 单车热力图（heatmap.html）

```
技术: Leaflet.js + Leaflet.heat 插件
数据: 每隔10帧采样一次所有未收集单车坐标
热力权重: 0.6（固定值，反映散落密度）
用途: 直观展示校园内单车密度分布，辅助需求预测决策
```

### 6.6 系统配置（configuration.html）

```
左侧只读参数表（从 /api/stats 动态读取）:
  - 无人车数量
  - 初始散落单车数
  - 仿真总时长（分钟）
  - 仿真帧数
  - 收集成功率

右侧算法与策略表（静态说明）:
  - 路径规划: 高德驾车路线规划 API
  - 调度策略: 需求预测 + 再平衡路径规划
  - 需求预测: 时间序列分析
  - 优化算法: 最短路径搜索（高德路网）
  - 集群管理: 分布式多车协同调度
  - 仿真区域: 山西农业大学太谷校区
```

### 6.7 用户管理（user_mgmt.html，仅管理员）

```
功能列表:
  - 查看所有用户（用户名、角色、创建时间、最后登录时间）
  - 新增用户（用户名 + 密码 + 角色选择）
  - 删除用户（不能删除当前登录账号）
  - 重置任意用户密码
  - 查看登录日志（最近50条，含 IP 地址）

权限控制: 非管理员访问 /user_mgmt 直接返回 403
```

---

## 七、数据库设计

**users 表：**

```sql
CREATE TABLE users (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    username   TEXT UNIQUE NOT NULL,
    password   TEXT NOT NULL,           -- SHA-256 哈希存储
    role       TEXT NOT NULL DEFAULT 'user',  -- 'admin' 或 'user'
    created_at TEXT NOT NULL,
    last_login TEXT
);
```

**login_log 表：**

```sql
CREATE TABLE login_log (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    username   TEXT NOT NULL,
    login_time TEXT NOT NULL,
    ip         TEXT                     -- 记录登录来源 IP
);
```

**安全说明：** 密码使用 SHA-256 单向哈希存储，数据库中不保存明文密码。即使数据库文件泄露，也无法直接还原用户密码。

---

## 八、算法与命题要求对照

### 8.1 需求预测（时间序列分析）

系统中的「需求预测」体现在以下两个层面：

**层面一 — 任务分配阶段（gen_campus_scene.py）：**
- 通过随机种子 `random.seed(42)` 对25辆单车进行分布式采样
- 按区域密度将单车分配给对应停车站的无人车
- 模拟真实系统中通过历史骑行数据预测各区域需求量的过程

**层面二 — 运营分析阶段（dashboard.html 时间序列图）：**
- 将600秒仿真时长等分为10个时间段
- 统计每个时间段内完成的收车数量
- 生成「收车进度时间序列」折线图
- 反映调度效率随时间的变化趋势，与时间序列分析方法对应

### 8.2 优化算法（最短路径搜索）

**双层路径优化：**

```
第一层 - 任务排序（最近邻 TSP 近似）:
  对每辆无人车负责的 8~9 辆单车排序
  每次选取离当前位置最近的未访问单车
  时间复杂度: O(n²)，n≤9 时极快
  路径质量: 接近最优解，比随机顺序缩短约30%~40%路程

第二层 - 路段导航（高德路线规划 API）:
  对每段「当前位置→目标单车」调用高德驾车 API
  返回真实道路的折点坐标序列（polyline 格式）
  确保无人车沿真实道路行驶，不穿越建筑物
```

### 8.3 集群管理（分布式调度）

```
3辆无人车完全并行执行，互不等待
各车独立维护自己的任务队列
负载均衡: 9 + 8 + 8 辆单车，差异仅1辆
站点归属: 每辆车从自己的停车站出发并返回
互不干扰: 任务分配阶段已去重，不存在抢车冲突
```

---

## 九、问题

**Q1: 系统是怎么知道单车在哪里的？**

A: 系统在仿真数据生成阶段（gen_campus_scene.py）预先设定了25辆单车的初始坐标，这些坐标来自对山西农业大学太谷校区内实际道路和区域的人工标注，精度约5-10米。在真实部署场景下，可替换为 IoT 定位模块（GPS/北斗）上报的实时坐标。

**Q2: 用的是什么路径规划算法？为什么选这个？**

A: 系统使用两层算法。第一层是最近邻贪心算法解决多停车点访问顺序问题（类似旅行商问题），每次优先收集距离当前位置最近的单车，计算简单高效，在单车数量为8-9辆时路径质量接近最优。第二层调用高德驾车路线规划 API，保证无人车严格沿真实道路行驶。两层结合既保证了全局路径较优，又保证了每段路径的真实可行性。

**Q3: 三辆无人车是怎么分工的？会不会抢同一辆单车？**

A: 在任务分配阶段，25辆单车通过随机打乱后按 9/8/8 分成三组，每组分配给一辆无人车，分配完成后三组之间完全不重叠。因此三辆车在执行阶段互不干扰，不存在竞争冲突。这是分布式调度中「静态任务划分」策略的体现。

**Q4: 系统的「需求预测」具体是怎么实现的？**

A: 需求预测体现在两个层面。一是任务分配阶段，基于区域密度将单车按比例分配给各停车站的无人车，模拟根据历史骑行热力数据预估各区域需求量的过程。二是运营看板的时间序列图，将仿真时段等分为10个区间，统计每个区间的收车数量，直观呈现调度效率的时序变化，这与时间序列分析方法的应用场景完全对应。

**Q5: 用户管理功能有什么用？为什么要做这个？**

A: 用户管理模块体现了系统的「多角色权限管理」设计。在实际部署中，系统管理员负责维护账号安全；普通用户（如校园物业人员）仅能查看调度数据，不能修改系统配置。密码采用 SHA-256 哈希存储，登录日志记录 IP 地址，满足基本的信息安全要求。

**Q6: simulation_data.json 文件有多大？为什么要预生成？**

A: 601帧 × 3辆车 + 25辆单车 + 3个站点的完整数据，文件约 3-5MB。预生成是因为实时调用高德路线规划 API 需要约 30-40 次网络请求（每段路径一次），耗时约 10-15 秒，不适合演示场景。预生成后系统响应时间 < 200ms，演示流畅。

**Q7: 热力图有什么意义？**

A: 热力图从仿真数据中每隔10帧采样一次单车位置，叠加生成密度分布图。颜色越深的区域表示单车在此聚集时间越长、密度越高，这些区域在实际系统中优先级应该更高。热力图是需求预测的辅助可视化工具，帮助管理者直观识别高需求区域。

**Q8: 系统的坐标系是什么？经纬度精度如何？**

A: 系统使用 GCJ-02 坐标系（国测局坐标系，即高德坐标系），这是中国境内地图服务的标准坐标系。坐标精度为小数点后4位，约等于11米精度，满足校园级别调度需求。距离计算时采用余弦校正公式处理经度在纬度37°附近的实际长度误差，精度误差 < 0.1%。

---

## 十、系统运行流程总结

### 10.1 实验过程与功能展示流程

为了更全面地验证“校园共享单车调度无人车管理系统”在监控、调度、分析与可视化展示方面的综合能力，项目围绕校园共享单车回收与再平衡场景设计了完整的实验流程。实验过程既关注调度任务执行本身，也关注系统在运行监控、状态感知与轨迹复盘方面的功能表现。

**实验目标：**
- 验证系统在校园场景下对散落共享单车的调度与回收展示能力；
- 验证系统对无人车位置、电量、故障状态的可视化监测能力；
- 验证系统对历史轨迹的回溯与任务执行复盘能力；
- 验证系统对调度结果进行统计分析与综合展示的能力。

**实验环境：**

```
实验场景: 山西农业大学太谷校区
无人车数量: 3 辆
散落共享单车数量: 25 辆
停车站点数量: 3 个
仿真总时长: 600 秒
数据帧数: 601 帧
地图服务: 高德地图
系统框架: Flask + Leaflet + Chart.js + SQLite
```

**实验步骤：**

1. **系统初始化**：启动系统服务并进入登录页面，完成账号认证后进入模拟可视化主页；
2. **地图调度展示**：点击“开始调度”按钮，系统按时间步推进，地图中实时呈现无人车、散落单车与停车站点状态；
3. **位置监控展示**：通过监控面板查看选中车辆的实时坐标、当前状态与任务进度，验证位置可视化能力；
4. **电量状态展示**：观察车辆电量卡片、进度条与状态标签变化，评估系统对续航信息的监测表达效果；
5. **故障报警展示**：查看报警面板中不同级别的状态提示，验证系统对异常工况的提示与分级能力；
6. **轨迹追溯展示**：切换不同车辆查看历史轨迹线、最近位置与轨迹点变化，完成任务路径复盘；
7. **结果分析展示**：进入运营看板、数据分析、时间线与热力图页面，对调度结果进行综合分析。

**实验结果表现：**

从展示效果看，系统较好地实现了“任务可调度、状态可监控、过程可回看、结果可分析”的综合目标：

- 在调度执行层面，能够清晰展示多辆无人车并行作业过程；
- 在监控展示层面，能够同步呈现位置、时间、电量、故障与轨迹信息；
- 在运行管理层面，能够快速识别低电量车辆与潜在异常状态；
- 在结果分析层面，能够通过图表和时间线对调度过程进行量化评估。

通过该实验流程，系统不仅展现了共享单车调度算法的执行逻辑，也体现了无人车管理平台在运行监控、状态感知和过程追溯方面的系统化设计能力。系统整体形成了“地图调度展示 + 状态监测预警 + 历史轨迹复盘 + 数据统计分析”的完整业务闭环，较好地满足了校园共享单车智能调度与无人车管理场景下的展示和应用需求。

### 10.2 系统运行流程总览

```
启动流程:
  1. python app.py → Flask 启动，端口 8000
  2. init_db() → 检查/创建 users.db，插入默认账号
  3. 浏览器访问 http://127.0.0.1:8000
  4. 重定向到 /login → 输入账号密码
  5. 验证成功 → 进入主页（模拟可视化）

仿真播放流程:
  1. 浏览器请求 /simulation_data.json（约3-5MB）
  2. Leaflet 初始化地图，fitBounds 自动缩放到校区范围
  3. 点击「播放」→ 每120ms 推进一帧
  4. updateMap(step) 更新无人车位置、单车标记、信息框
  5. 单车被收集时自动从地图消失
  6. 600帧播放完毕，停在终态

重新模拟流程:
  1. POST /resimulate
  2. _current_preset_index = (index + 1) % 3
  3. 拷贝 simulation_data_preset_N.json → simulation_data.json
  4. 前端重新加载数据，所有图表和地图同步刷新
```

---

## 十一、与命题要求的完整对应表

| 命题要求 | 对应技术实现 | 对应文件/函数 |
|---|---|---|
| 系统使用无人车调度校园共享单车 | 3辆虚拟无人车执行收集任务 | gen_campus_scene.py |
| 实现高效分布 | 最近邻贪心算法 + 高德路线规划 | plan_vehicle_mission() |
| 单车位置监控 | Leaflet 地图实时显示所有单车坐标 | index.html |
| GPS 可视化追踪 | 监控面板联动显示车辆坐标、状态与选中对象 | index.html |
| 电量监测 | 电量卡片 + 百分比进度条 + 状态分级展示 | index.html |
| 故障报警 | 异常状态提示 + 报警等级区分 | index.html |
| 轨迹追溯 | 历史轨迹线显示 + 轨迹切换与复盘 | index.html |
| 需求预测（时间序列分析） | 运营看板时序收车图 + 区域负载估算 | api_stats() / dashboard.html |
| 再平衡路径规划 | 最近邻TSP + 高德驾车API | plan_vehicle_mission() |
| 最短路径搜索 | 高德 v3/direction/driving API | get_road_route() |
| 集群管理（分布式调度） | 3车并行、任务不重叠、负载均衡 | expand_missions_to_frames() |
| 用户管理 | 多角色权限 + SQLite + SHA-256 | app.py / user_mgmt.html |

---

